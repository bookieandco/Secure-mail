# MAIL-1 isolated configuration. No secrets belong in this file.
# Copy to a local, untracked environment file only when local deployment is ready.

LOG_LEVEL=info
SUPERVISOR_LOGLEVEL=warn
ACCOUNT_PROVISIONER=FILE
PERMIT_DOCKER=none
ENABLE_UPDATE_CHECK=0

# Security services are enabled by the DMS image.
ENABLE_RSPAMD=1
ENABLE_CLAMAV=1
ENABLE_FAIL2BAN=0

# Require TLS for authenticated submission. Final production TLS policy is MAIL-3.
SSL_TYPE=manual

# MAIL-1 does not publish management interfaces or data services.
